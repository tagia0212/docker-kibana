server {
  listen   80; ## listen for ipv4
  listen   [::]:80 default ipv6only=on; ## listen for ipv6

  access_log  /var/log/nginx/ssl.redirector.access.log;

  location / {
    rewrite ^ https://$host$uri permanent;
  }
}
